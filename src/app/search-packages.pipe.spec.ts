import { SearchPackagesPipe } from './search-packages.pipe';

describe('SearchPackagesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPackagesPipe();
    expect(pipe).toBeTruthy();
  });
});
